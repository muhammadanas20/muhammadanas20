# Challenge — Phase 4: Docker

This is optional. It is also how you get interesting interview stories.

## Reproduce CI locally

A GitHub Action builds the image and runs pytest inside it. Same command works locally.

**Constraints**

- No extra undocumented env
- Python version matches

**Success looks like**

A failing test fails in both places the same way.
