# Quiz — Phase 6: Embeddings and search

Closed notes. 80% to pass. Answers at the bottom. No scrolling first.

1. An embedding is
    A) A JPEG of the text
    B) A vector representing meaning
    C) A Git commit
    D) A JWT
2. Query with a different embedding model
    A) Is faster
    B) Is invalid / incomparable
    C) Is required
    D) Increases recall always
3. Cosine similarity cares about
    A) Mostly direction/angle
    B) Only vector length
    C) File size
    D) CPU vendor
4. A good reason for overlap
    A) It looks professional
    B) Sentences spanning a cut stay together
    C) It reduces storage
    D) It trains the LLM
5. Error code ECONNRESET is best found with
    A) Only 3072-d embeddings
    B) Keyword / exact
    C) Temperature 2
    D) Docker
6. Chunking through a table is
    A) Fine
    B) Usually destructive
    C) Required
    D) A kind of embedding
7. k in kNN is
    A) The vector dimension
    B) How many neighbors to return
    C) The learning rate
    D) Token count
8. Re-embed the corpus when
    A) You change the embedding model
    B) You change CSS
    C) You add a JWT
    D) Never
9. Metadata is for
    A) Filters and citations
    B) Replacing vectors
    C) Training GPT
    D) TLS
10. Brute-force cosine is
    A) O(N D)
    B) O(1)
    C) O(log N) always
    D) Impossible

---

<details>
<summary>Answers (spoiler)</summary>

1. **B** — Geometry of meaning.
2. **B** — Different spaces.
3. **A** — Angle.
4. **B** — Boundary context.
5. **B** — Identifiers.
6. **B** — Keep tables whole.
7. **B** — Top-k.
8. **A** — New space.
9. **A** — Beside the vector.
10. **A** — Until ANN.

</details>
