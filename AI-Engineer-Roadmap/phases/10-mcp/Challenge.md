# Challenge — Phase 10: Model Context Protocol (MCP)

This is optional. It is also how you get interesting interview stories.

## Wrap your Phase 8 retriever as MCP

search + get_chunk(id) tools. Agent in Phase 9 uses them instead of in-process Python.

**Constraints**

- Same eval questions still pass

**Success looks like**

The protocol boundary didn't destroy quality.
