[project]
name = "ai-engineer-roadmap"
version = "0.1.0"
description = "Free production-first AI engineering course"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["EXAMPLES"]
