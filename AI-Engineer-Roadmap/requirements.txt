# Core — used across phases. Install in a venv:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# You do not need every extra extra at once. Uncomment as you reach those phases.

# --- Phases 1–3 ---
pydantic>=2.7
httpx>=0.27
fastapi>=0.115
uvicorn[standard]>=0.30
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# --- Phase 2 ---
sqlalchemy>=2.0
psycopg[binary]>=3.1
redis>=5.0
alembic>=1.13

# --- Phase 5–8 (local-friendly) ---
numpy>=1.26
tiktoken>=0.7
pypdf>=4.2

# --- Tests / lint ---
pytest>=8.2
ruff>=0.5

# --- Optional (uncomment when needed) ---
# openai>=1.40
# anthropic>=0.34
# sentence-transformers>=3.0
# chromadb>=0.5
# qdrant-client>=1.9
# pgvector>=0.3
# ragas>=0.1
# deepeval>=1.0
# langfuse>=2.40
# mcp>=1.0
